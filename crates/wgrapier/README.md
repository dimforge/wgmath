# wgrapier: cross-platform GPU physics simulation

**/!\ This library is still under heavy development and is still missing many features.**

The goal of **wgrapier** is to especially be "**rapier** on the gpu". It aims (but it isn’t there yet) to be a
GPU rigid-body physics engine.
